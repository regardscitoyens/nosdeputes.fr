#!/bin/bash

loi=$1

. $(echo $0 | sed 's/[^\/]*$//')db.inc
cd $PATH_APP

mkdir -p web/amendements/$loi

for format in xml json csv; do
  php symfony dump:AmendementsLoi $loi $format > web/amendements/$loi/nossenateurs.fr-amendements-$loi.$format;
done

